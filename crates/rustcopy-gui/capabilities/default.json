{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Native file pickers and a completion toast for the main window. Nothing here grants filesystem read or write: the dialog plugin returns the path a person selected, every read still goes through a command backed by robocopy_ingest::gui_api, and the notification plugin only ever posts a summary of a run the console itself started.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save", "notification:default"]
}
